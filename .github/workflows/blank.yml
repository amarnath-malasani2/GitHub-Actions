name: Git Checkout Code

on:
  push:

jobs:
  checkout:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the Code
        uses: actions/checkout@v4

      - name: Print amar file
        run: cat amar
